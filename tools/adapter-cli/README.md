# Adapter CLI

Command-line entrypoint for adapter inspection and future flow validation.

Current:

- `node tools/adapter-cli/index.js list`

Planned:

- validate flow files against adapter capabilities
- replay recorded fixtures
- scaffold new adapter packages
