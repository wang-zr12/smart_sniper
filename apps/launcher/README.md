# Launcher

Local process entrypoint for the Windows MVP.

Current:

- starts the API/dashboard server

Planned:

- Vault daemon supervision
- long-path startup probe
- optional Windows Service registration after `OD-005` is resolved
